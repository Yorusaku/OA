# 阶段 8 Prompt：联调、异常处理与收尾（Demo 打磨）

> 用途：交给 Agent 的完整指令，让它只实现「阶段 8」——集中处理接口联调、统一异常处理、登录过期逻辑、必要文档和 Demo 打磨，让项目达到“可以自信放进简历和对着演示”的状态。

```text
你现在是编码 Agent，请严格按照下面项目说明和约束开发代码。

【一、项目背景】
- 项目名称：全景智能 OA 协同办公平台（企业级 OA / 人事协同中台）。
- 当前状态：阶段 0～7 已基本完成，包括：
  - monorepo+turbo 基建、权限引擎、DynamicForm、WorkflowCanvas、Vue Query 状态架构、业务模块页面、性能优化等。
- 本轮目标是完成「联调与收尾」：整合已有能力、统一异常处理、登录过期逻辑和最小但完整的 Demo 流程，以及基础文档。

【二、本次要完成的阶段/范围】
- 仅实现《开发计划》中 **阶段 8：联调、异常与收尾**，重点包含：

1. 统一异常与消息处理
   - 在 API 层或全局拦截中统一处理：
     - 网络错误、后端错误码（如非 0 / 非 200）；
     - 使用 Element Plus 的 Message 或 Notification 做用户可见的错误提示；
   - 对 Vue Query 的 `onError` 进行统一封装：
     - 封装一个通用的错误处理函数，被各个 `useQuery` / `useMutation` 复用；
     - 避免组件里到处写重复的错误处理逻辑。

2. 登录过期（401）统一处理
   - 在 HTTP 拦截器中拦截 401：
     - 清空 Pinia 中的 Token、用户信息和权限状态；
     - 跳转到登录页；
     - 可选：根据情况弹出“登录过期，请重新登录”的提示。
   - 确保任何接口触发 401 时都能走这套逻辑，而不是在每个 API 调用点单独判断。

3. 最小可演示闭环（Demo Flow）
   - 打磨至少 1 条「端到端」演示路径，使其从登录 → 进入工作台/审批中心 → 发起一个简单流程 → 在审批中心看到该流程 → 查看详情 的链路是顺畅的。
   - 如后端暂未完全就绪，可：
     - 使用前端 mock / MSW 等方案，但保持接口形式与计划中的 REST 约定一致；
     - 或在有限范围内直连真实后端，并在 README 中说明依赖的后端服务。

4. 基础文档与 README
   - 在仓库根目录编写或完善 `README.md`，内容建议包括：
     - 项目简介与核心亮点（动态表单引擎、可视化工作流、Vue Query 状态架构、monorepo+turbo 等）；
     - 技术栈清单；
     - 目录结构简要说明；
     - 本地运行指南（依赖、安装、启动命令、环境变量说明）；
     - Demo 账号/登录方式说明（如有）。
   - 如有必要，可在 `docs/` 或 `plan/` 里补充简短的“架构说明”或“模块说明”文档。

5. 小范围代码清理
   - 删除明显无用的死代码、调试日志；
   - 对明显重复的逻辑做轻量抽取（在不大改架构的前提下）。

【三、实现与代码质量要求】
- 不要在这个阶段引入新的大功能或大重构，重点是「打磨」和「统一」：
  - 异常处理路径要清晰、一致；
  - 登录过期逻辑要可靠；
  - Demo 流程要稳定可演示。
- README 与文档要尽量简洁直观，重点突出你在简历和面试中可讲的亮点，而不是铺陈一堆细节。

【四、你在这轮需要输出什么】
- 列表说明：
  - 统一异常处理逻辑位于哪些文件（路径 + 职责）；
  - 登录过期（401）处理路径与相关 store / 路由逻辑；
  - README 的主要内容概览（可以直接给出 README 的全文）。
- 如果你为 Demo Flow 做了特别的 mock 或配置，也需要说明放在哪个文件、如何启用/关闭。

【五、工作方式】
- 第一步：用不超过 10 行话复述你对本轮任务（阶段 8）的理解。
- 第二步：列出 3～6 条你将要执行的步骤（例如「1. 统一异常与消息处理」「2. 实现 401 登录过期处理」「3. 打磨端到端 Demo 流程」「4. 编写/完善 README」「5. 清理明显死代码」等）。
- 第三步：按步骤执行，边改代码边简洁说明关键进度和设计决策。

现在，请你从阅读并理解上述约束开始，先复述你对本轮任务（阶段 8）的理解，然后给出你的执行步骤，再开始真正修改代码。
```
