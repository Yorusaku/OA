# 阶段 9 Prompt：代码质量与架构优化（类型安全 + 代码复用 + 错误处理）

&gt; 用途：交给 Agent 的完整指令，让它只实现「阶段 9」——提升代码质量、增强类型安全、消除代码重复、完善错误处理，为项目建立坚实的质量保障体系。

```text
你现在是编码 Agent，请严格按照下面项目说明和约束开发代码。

【一、项目背景】
- 项目名称：全景智能 OA 协同办公平台（企业级 OA / 人事协同中台）。
- 当前状态：阶段 0～8 已完成，包括：
  - monorepo+turbo 基建、权限引擎、DynamicForm、WorkflowCanvas、Vue Query 状态架构、业务模块、性能优化、Demo 打磨。
- 本轮目标：代码质量与架构优化，建立类型安全屏障，消除代码重复，完善错误处理，提升项目可维护性。

【二、技术与优化方向约束】
- 类型安全方向：
  - 完善所有模块的 TypeScript 类型定义，达到全链路类型安全；
  - 引入 zod 或 yup 对 API 响应和表单数据进行运行时类型校验；
  - 消除 any 和 unknown 类型的滥用，使用类型守卫和类型推断。
- 代码复用方向：
  - 提取公共逻辑到 composables，消除 DynamicForm 和 useDynamicValidate 中的重复代码；
  - 创建通用的表单字段 composable（useFormField）统一处理字段状态；
  - 建立可复用的组件库基础结构。
- 错误处理方向：
  - 完善全局 API 错误拦截器，统一处理各种错误类型；
  - 为 Vue Query mutations 添加统一的错误处理机制；
  - 实现错误边界组件，优雅处理渲染错误。

【三、本次要完成的阶段/范围】
仅实现《开发计划》中 **阶段 9：代码质量与架构优化**，包含：

1. 类型安全增强
   - 完善 `@/types/form-schema.ts` 和 `@/types/workflow.ts` 的类型定义：
     - 为所有字段添加严格的类型约束，消除 optional 滥用；
     - 定义类型守卫函数，确保运行时类型安全；
     - 为表单字段类型定义枚举，避免字符串魔法值。
   - 引入 zod 库，对 API 响应数据进行运行时校验：
     - 为各个 API 模块定义 zod schema；
     - 在 http.ts 中添加响应数据的 zod 校验层；
     - 校验失败时有明确的错误提示和日志记录。
   - 消除代码中的 any 和 unknown 类型：
     - 使用类型断言替代 any（在必要时）；
     - 为组件 props 和 emits 定义完整的类型；
     - 为自定义 hooks 的返回值定义精确的类型。

2. 代码复用与逻辑抽取
   - 消除 DynamicForm.vue 和 useDynamicValidate.ts 中的重复代码：
     - 将 checkCondition、checkConditions、getConditionFields 等条件判断逻辑抽取到 `@/utils/form-conditions.ts`；
     - 两个模块都从同一个工具文件导入，保持逻辑一致。
   - 创建 `useFormField` composable（位于 `@/composables/useFormField.ts`）：
     - 封装字段的状态管理、值变更、错误处理等逻辑；
     - 支持字段的可见性、禁用状态、必填性等计算；
     - 提供统一的字段事件处理接口。
   - 建立共享工具函数库（`@/utils/` 目录）：
     - `@/utils/constants.ts` - 存放常量定义；
     - `@/utils/validators.ts` - 通用校验函数；
     - `@/utils/formatters.ts` - 数据格式化函数。

3. 全局错误处理完善
   - 增强 `@/api/http.ts` 的错误拦截器：
     - 区分网络错误、后端业务错误、超时错误等不同类型；
     - 为不同错误类型提供友好的用户提示；
     - 记录错误日志，方便排查问题。
   - 封装 Vue Query 错误处理（位于 `@/composables/useQueryErrorHandler.ts`）：
     - 提供统一的 `onQueryError` 和 `onMutationError` 处理函数；
     - 支持自定义错误提示和回调；
     - 自动处理 401 等特殊错误码。
   - 创建错误边界组件 `@/components/ErrorBoundary.vue`：
     - 捕获子组件渲染错误，避免白屏；
     - 显示友好的错误提示和重试按钮；
     - 记录错误信息便于排查。

4. 代码规范与工具链优化
   - 完善 ESLint 配置：
     - 添加 TypeScript 相关规则（`@typescript-eslint/no-explicit-any`、`@typescript-eslint/strict-boolean-expressions`）；
     - 添加 Vue 3 最佳实践规则；
     - 配置自动修复规则，确保代码一致性。
   - 配置 Prettier 格式化规则：
     - 统一缩进、引号、分号等格式；
     - 与 ESLint 规则保持一致；
     - 确保 Git 提交时自动格式化。

【四、实现与代码质量要求】
- 类型安全必须是"可验证"的：
  - 运行 TypeScript 编译（`npm run typecheck`）无错误；
  - 所有 API 响应都通过 zod 校验，防止脏数据污染前端；
  - 类型定义要有注释说明，方便团队理解。
- 代码复用必须是"渐进式"的：
  - 先抽取明显的重复逻辑，避免过度设计；
  - 保持现有 API 稳定，不破坏已有功能；
  - 抽取后的代码要有单元测试（可选，但建议）。
- 错误处理必须是"用户友好"的：
  - 错误提示要清晰明确，让用户知道该怎么做；
  - 技术错误不要直接暴露给用户，需要转换为友好的文案；
  - 关键错误要有日志记录，方便开发人员排查。

【五、你在这轮需要输出什么】
- 明确列出：
  - 新增/修改的类型定义文件（路径 + 说明）；
  - 抽取的公共工具函数和 composables（路径 + 职责）；
  - 完善的错误处理逻辑相关文件；
  - 配置文件的调整（ESLint、Prettier 等）。
- 给出核心类型定义、抽取的公共逻辑、错误处理机制的关键代码。

【六、工作方式】
- 第一步：用不超过 10 行话复述你对本轮任务（阶段 9）的理解。
- 第二步：列出 3～6 条你将要执行的步骤（例如「1. 完善类型定义」「2. 抽取公共逻辑」「3. 完善错误处理」「4. 优化代码规范配置」等）。
- 第三步：按步骤执行，边改代码边简洁说明关键进度和设计决策。

现在，请你从阅读并理解上述约束开始，先复述你对本轮任务（阶段 9）的理解，然后给出你的执行步骤，再开始真正修改代码。
```
