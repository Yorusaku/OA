# 阶段 7 Prompt：性能与体验优化（虚拟滚动 / 懒加载 / Tailwind+Element 统一）

> 用途：交给 Agent 的完整指令，让它只实现「阶段 7」——围绕万级组织架构树、通讯录长列表、样式统一与打包体验，完成性能与体验优化。

```text
你现在是编码 Agent，请严格按照下面项目说明和约束开发代码。

【一、项目背景】
- 项目名称：全景智能 OA 协同办公平台（企业级 OA / 人事协同中台）。
- 目前仓库已完成阶段 0～6：架构、权限、Vue Query、动态表单、流程编排以及主要业务页面。
- 本轮目标是针对「长列表与长树渲染」和「UI 统一性」做一次集中性能与体验优化，为简历提供有说服力的性能优化案例。

【二、技术与优化方向约束】
- 性能方向：
  - 使用 VueUse（如 `useVirtualList`、`useIntersectionObserver`）优化长列表与懒加载；
  - 必要时通过数据扁平化、分批加载减少 DOM 数量。
- UI 方向：
  - 使用 Tailwind Design Tokens 深度覆写 Element Plus 的 CSS Variables，使两者在色彩、圆角、间距上统一；
  - 减少冗余 CSS，自定义样式尽量通过原子类与变量实现。
- 构建与体验方向：
  - 路由懒加载、大组件异步加载；
  - Vue Query 的 `staleTime`、`cacheTime` 等参数调优，减少重复请求。

【三、本次要完成的阶段/范围】
- 仅实现《开发计划》中 **阶段 7：性能与体验优化**，包含：

1. 万级组织架构树优化
   - 针对 `组织架构` 模块：
     - 将组织架构树的原始嵌套数据转换为扁平结构（如一个数组 + parentId 表示层级）；
     - 使用 VueUse 的 `useVirtualList` 或类似思路，在页面上只渲染可视区域内的节点；
     - 确保滚动时仍能保持树形层级的视觉结构（可以适度简化交互，只要能证明虚拟化生效即可）。

2. 通讯录长列表优化
   - 针对 `通讯录` 模块：
     - 使用 `useVirtualList` 优化人员列表的渲染，只渲染当前可见的若干行；
     - 对复杂的人员卡片（含头像、标签等，如已实现）可以结合 `useIntersectionObserver` 实现懒加载：进入视口前只占位，进入后再加载详情或头像。

3. 异步组件与懒加载
   - 对一些重量级组件（例如流程编辑器、复杂表单页面），通过 `defineAsyncComponent` 或路由层面的异步组件实现懒加载；
   - 确保首屏加载体积与加载时间得到优化。

4. Tailwind + Element Plus 主题统一
   - 在 `apps/web/tailwind.config.js` 中梳理 Design Tokens（例如 primary/secondary 颜色、圆角、阴影、字号等）；
   - 在全局样式或 Element Plus 主题配置中，利用这些 Token 覆写 Element Plus 的 CSS 变量（如 `--el-color-primary`、`--el-border-radius-base` 等）；
   - 目标：减少到处写「魔法色值/圆角」，统一从 Tailwind Token 出发。

5. Vue Query 参数与体验调优
   - 检查高频使用的 Vue Query hooks（如字典、部门树、用户信息、待办统计等）：
     - 为明显只读且变化不频繁的数据设置较长 `staleTime`，减少重复请求；
     - 对频繁交互的数据设置适当的 `refetchOnWindowFocus`、`refetchOnReconnect` 策略；
   - 不必大改接口，只需调整配置并测试行为是否符合预期。

【四、实现与代码质量要求】
- 性能优化必须是「可验证」的：
  - 对虚拟列表/懒加载相关的实现，建议在注释中说明原理和使用方法，方便我自己打开页面时能明显感知效果；
  - 不要引入过于复杂、难以维护的优化手段（比如手写一大堆极度 hack 的滚动逻辑）。
- 样式层面尽量通过 Tailwind 和 CSS 变量完成，不要大量添加新的自定义 CSS 文件。
- 如需对现有组件做结构性调整以配合虚拟列表/懒加载，请保持 API 稳定（props/emits 尽量不变），避免破坏已有业务逻辑。

【五、你在这轮需要输出什么】
- 明确列出：
  - 哪些页面/模块做了虚拟列表或懒加载改造（路径 + 说明）；
  - 在 `tailwind.config.js` 和 Element Plus 主题相关文件中做了哪些 Design Tokens 与变量映射调整；
  - 哪些 Vue Query hooks 的配置被调优（以及调整意图）。
- 给出核心虚拟列表实现、懒加载逻辑以及 Tailwind+Element 主题映射的关键代码。

【六、工作方式】
- 第一步：用不超过 10 行话复述你对本轮任务（阶段 7）的理解。
- 第二步：列出 3～6 条你将要执行的步骤（例如「1. 改造组织架构树为虚拟列表」「2. 优化通讯录长列表」「3. 抽象 Tailwind Design Tokens 并映射 Element 变量」「4. 调整 Vue Query 配置」等）。
- 第三步：按步骤执行，边改代码边简洁说明关键进度和设计决策。

现在，请你从阅读并理解上述约束开始，先复述你对本轮任务（阶段 7）的理解，然后给出你的执行步骤，再开始真正修改代码。
```
