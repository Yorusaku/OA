# 阶段 11 Prompt：DevOps、测试与文档完善（自动化 + 测试 + 文档）

&gt; 用途：交给 Agent 的完整指令，让它只实现「阶段 11」——建立自动化测试体系、完善开发文档、配置 CI/CD 流程，让项目达到生产级别的质量标准。

```text
你现在是编码 Agent，请严格按照下面项目说明和约束开发代码。

【一、项目背景】
- 项目名称：全景智能 OA 协同办公平台（企业级 OA / 人事协同中台）。
- 当前状态：阶段 0～10 已完成，包括：
  - 完整的架构、权限引擎、动态表单、工作流引擎、性能优化、代码质量提升、功能扩展等。
- 本轮目标：建立自动化测试体系、完善开发文档、配置 CI/CD 流程，为项目提供质量保障和可维护性。

【二、技术与优化方向约束】
- 测试方向：
  - 使用 Vitest 进行单元测试和组件测试；
  - 使用 Playwright 进行端到端测试；
  - 测试覆盖率目标：核心模块 80% 以上。
- 文档方向：
  - 为所有核心组件和 composables 添加 JSDoc 注释；
  - 建立 Storybook 组件库文档；
  - 完善 API 文档和架构文档。
- CI/CD 方向：
  - 配置 GitHub Actions 实现自动化构建和测试；
  - 添加代码质量检查和测试覆盖率报告；
  - 配置自动化部署流程。

【三、本次要完成的阶段/范围】
仅实现《开发计划》中 **阶段 11：DevOps、测试与文档完善**，包含：

1. 自动化测试体系
   - 单元测试（Vitest）：
     - 为所有 composables 编写单元测试（`useApproval`、`useWorkflow`、`useDynamicValidate` 等）；
     - 为工具函数编写单元测试（`form-conditions`、`validators`、`formatters` 等）；
     - 配置 Vitest 的测试覆盖率报告，目标核心模块 80% 以上。
   - 组件测试（Vitest + Vue Test Utils）：
     - 为核心组件编写测试（`DynamicForm`、`WorkflowCanvas`、`MainLayout` 等）；
     - 测试组件的 props、emits、slot 和交互逻辑；
     - 使用 mock 数据隔离组件依赖。
   - 端到端测试（Playwright）：
     - 配置 Playwright 测试环境；
     - 编写关键流程的 E2E 测试（登录、发起审批、审批流程等）；
     - 支持多浏览器测试（Chrome、Firefox、Safari）。

2. 组件文档与 Storybook
   - 集成 Storybook：
     - 在 `apps/web` 中安装和配置 Storybook；
     - 创建 stories 目录结构（`stories/`）；
     - 配置 Storybook 的主题和插件。
   - 编写组件 stories：
     - 为所有公共组件编写 stories（`DynamicForm`、`WorkflowCanvas`、表单字段组件等）；
     - 展示组件的不同状态和使用场景；
     - 添加 controls 支持交互式调整组件 props。
   - 生成组件文档：
     - 配置 Storybook 的 Docs 插件；
     - 自动从 JSDoc 注释生成文档；
     - 添加使用示例和最佳实践说明。

3. 代码文档与注释
   - JSDoc 注释完善：
     - 为所有 composables 添加详细的 JSDoc 注释（参数、返回值、使用示例）；
     - 为所有工具函数添加 JSDoc 注释；
     - 为核心组件的 props 和 emits 添加 JSDoc 注释。
   - 架构文档：
     - 在 `docs/` 目录创建架构文档（`architecture.md`）；
     - 说明项目的整体架构设计和技术选型；
     - 绘制核心流程图（数据流转、权限校验、表单渲染等）。
   - API 文档：
     - 在 `docs/` 目录创建 API 文档（`api.md`）；
     - 说明前端 API 层的设计和使用方式；
     - 列出所有 API 接口的参数和返回值。
   - 开发指南：
     - 在 `docs/` 目录创建开发指南（`development.md`）；
     - 说明如何添加新功能、新组件、新页面；
     - 说明代码规范和最佳实践。

4. CI/CD 配置
   - GitHub Actions 配置：
     - 在 `.github/workflows/` 目录创建 CI 配置文件（`ci.yml`）；
     - 配置代码检查（ESLint、Prettier）；
     - 配置 TypeScript 类型检查；
     - 配置自动化测试（单元测试、组件测试）；
     - 配置测试覆盖率报告上传（Codecov 或类似服务）。
   - 构建优化：
     - 配置生产构建的缓存策略；
     - 优化构建输出，减小包体积；
     - 配置构建产物的版本管理。
   - 自动化部署（可选）：
     - 配置自动化部署到静态托管服务（Vercel、Netlify 或 GitHub Pages）；
     - 配置预览环境，每 PR 自动部署预览版本。

【四、实现与代码质量要求】
- 测试必须是"可维护"的：
  - 测试用例要清晰易读，使用描述性的测试名称；
  - 测试要独立，不依赖执行顺序；
  - 测试要快速，避免不必要的等待。
- 文档必须是"实用"的：
  - JSDoc 注释要简洁明了，重点说明"为什么"而不是"是什么"；
  - Storybook stories 要覆盖组件的主要使用场景；
  - 架构文档要图文并茂，方便理解。
- CI/CD 必须是"可靠"的：
  - CI 流程要快速，优先运行重要的检查；
  - 失败时要有明确的错误信息；
  - 配置缓存，加快后续执行速度。

【五、你在这轮需要输出什么】
- 明确列出：
  - 新增的测试文件（路径 + 说明）；
  - 新增的文档文件（路径 + 内容概览）；
  - CI/CD 配置文件（路径 + 配置说明）；
  - 测试覆盖率目标和实际结果。
- 给出核心测试用例、Storybook stories、CI/CD 配置的关键代码。

【六、工作方式】
- 第一步：用不超过 10 行话复述你对本轮任务（阶段 11）的理解。
- 第二步：列出 3～6 条你将要执行的步骤（例如「1. 搭建测试框架」「2. 编写单元测试」「3. 集成 Storybook」「4. 完善文档」「5. 配置 CI/CD」等）。
- 第三步：按步骤执行，边改代码边简洁说明关键进度和设计决策。

现在，请你从阅读并理解上述约束开始，先复述你对本轮任务（阶段 11）的理解，然后给出你的执行步骤，再开始真正修改代码。
```
